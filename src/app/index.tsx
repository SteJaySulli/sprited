import * as React from 'react';
import {render} from 'react-dom';
import {Layout} from '../components/layout';
import "../styles/app.scss";

render(<Layout></Layout>, document.getElementById('app'));
